// Interaction reach is shared by the authoritative server and the browser's
// target preview. Keeping these values in one module prevents the client from
// advertising an action that the server will reject.
export const INTERACTION_RANGE = 2.8;
export const ROLE_TARGET_RANGE = 3.2;
